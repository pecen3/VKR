const Router = require('express');
const authController = require('../controllers/authController')
const router = new Router();


// router.get('/login', authController.loginJWT);
// router.get('/reg', authController.regUser);
// router.post('/api', authController.apiCheck);
// router.post('/xls', authController.importXls);


module.exports = router;
